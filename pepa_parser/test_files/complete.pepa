/* A model containing all the possible constructs */

// The comment is the first construct to test.
r = 1.0 + 2.0 * 3 / 1.5;
/* Prefix */
P1 = (a, 1).P2;
/* Choice and rate expression */
P2 = (b, r + 3.0).P1 + (ciao, infty).P1;
P3 = P1 <a> P2; /* Cooperation */
P4 = P1 <> P1;
P5 = P1 || (P2 || P3);// OK?
H = P1[5]{a,b,c}; //OKKKKK
Hiding = P1/<a,b,c> + P2 + P3;
P4